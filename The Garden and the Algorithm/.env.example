# Vercel project → Environment Variables (never commit real keys)
ELEVENLABS_API_KEY=
FAL_KEY=
# Comma-separated extra allowed origins (optional)
# ALLOWED_ORIGINS=https://example.com,http://127.0.0.1:5500
